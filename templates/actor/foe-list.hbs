{{#> FoeList}}
{{#each list as |item key| }}
<div class="foe-item" data-item-id="{{item.id}}">
  {{{item.chargedSquare}}} <label type='text' {{#if item.available}}class="rollable" data-action="sendToChat"
    data-additional="{{item.system.description}}" {{/if}}>{{item.name}}</label>
  <desc>{{item.chargeUpText}}</desc>
  {{{item.system.description}}}
</div>
{{/each}}
{{/FoeList}}